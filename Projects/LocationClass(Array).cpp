#include<iostream>
#include<string.h>
#include <Location.h>
#include <Node.H>

using namespace std;

class Locations {
private:
	node *locations[10]; // database array
	public:
	Locations();
	void loadLocation(Location refNum) {
	private:
		Location locations[10];

	void loadLocations(Location refNum) {

	}
};

